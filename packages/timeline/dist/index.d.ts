import { JSX } from 'solid-js';

/** `YYYY-MM-DD` — строка, а не `Date`: часовые пояса сдвигают сутки */
type Day = string;
/** Отрезок на шахматке. Моменты — `YYYY-MM-DD` или `YYYY-MM-DDTHH:mm`. */
type Span = {
    id: string;
    /** в какой строке */
    row: string;
    from: string;
    /** конец: у суточной брони это день выезда */
    to: string;
};
/**
 * Сколько КАЛЕНДАРНЫХ суток между датами. Время игнорируется намеренно: это
 * «сколько ночей», а не «сколько часов».
 *
 * `PlainDate` не знает часовых поясов — значит переход на летнее время и
 * прочая зонная механика тут не существуют в принципе, а не «не мешают».
 */
declare const daysApart: (a: Day, b: Day) => number;
/** сдвинуть дату на N суток */
declare const shiftDay: (day: Day, n: number) => Day;

/** `YYYY-MM-DD` или `YYYY-MM-DDTHH:mm` — время необязательно */
type Moment = string;
/**
 * Правила ОДНОГО ресурса — сверх того, что задаёт общая шкала.
 *
 * Сетка у шахматки одна, а торгуют строки по-разному: баня — сеансами не короче
 * двух часов и с получасом уборки после, пейнтбол — от часа и с часом на
 * перезарядку, банкетный зал открыт с 14:00, хотя сетка начинается в 10:00.
 * Всё это — свойства строки, а не сетки, поэтому и живут они на строке.
 */
type RowRules = {
    /** короче не продаём, мин: сеанс бани — 120 */
    minMin?: number;
    /** зазор до соседей, мин: уборка после бани 30, перезарядка пейнтбола 60 */
    gapMin?: number;
    /** своё рабочее окно, минуты от полуночи: банкетный зал 14:00…23:00 */
    openMin?: number;
    closeMin?: number;
};
type Scale = {
    /** первый день сетки, `YYYY-MM-DD` */
    first: string;
    /** сколько дней показываем */
    days: number;
    /** минуты от полуночи: начало и конец рабочего окна дня */
    dayStart: number;
    dayEnd: number;
    /** самая короткая бронь на всей сетке, мин; у строки бывает своя (`RowRules`) */
    minMin?: number;
    /** сколько минут в КОЛОНКЕ: 1440 — сутки, 60 — час. Это про сетку, не про снап */
    stepMin: number;
    /**
     * Шаг, которым двигаются брони, мин. Не задан — равен колонке, и так в
     * подавляющем большинстве случаев: сетка бывает либо суточная, либо
     * почасовая, и двигать надо ровно по её делениям.
     *
     * Отдельный шаг нужен там, где единица продажи крупнее деления сетки:
     * например, сеанс продаётся строго по два часа, а часы в шапке всё равно
     * нужны, чтобы читать время.
     */
    snapMin?: number;
    /** ширина колонки, px */
    colW: number;
    /** во что превращать дату без времени: заезд и выезд */
    checkIn?: number;
    checkOut?: number;
};
/** минуты от полуночи; у даты без времени их нет — вернём `null` */
declare function minutesOf(m: Moment): number | null;
/**
 * Момент → минуты по шкале, где ночи вне рабочего окна вырезаны.
 *
 * `edge` решает судьбу даты БЕЗ времени: у начала это час заезда, у конца — час
 * выезда. Без этого гостиничная бронь `2026-06-01 … 2026-06-03` рисовалась бы
 * от полуночи до полуночи, то есть врала бы на полсуток с каждого края.
 */
declare function toMinutes(m: Moment, s: Scale, edge?: 'from' | 'to'): number;
/** минуты по шкале → пиксели */
declare const toX: (min: number, s: Scale) => number;
/** момент → пиксели, одним движением */
declare const momentX: (m: Moment, s: Scale, edge?: "from" | "to") => number;
/** шаг перемещения: не задан — равен колонке */
declare const snapOf: (s: Scale) => number;
/**
 * Самая короткая бронь, какая тут бывает, в минутах.
 *
 * Не равна шагу сетки, и это не мелочь: на суточной сетке шаг — 1440 минут, а
 * бронь на ОДНУ НОЧЬ при заезде в 16:00 и выезде в 12:00 длится 1200. Сравнение
 * с шагом отбраковывало её как «слишком короткую», и полосу нельзя было сжать
 * до одних суток — минимумом оказывались двое.
 */
declare function minLength(s: Scale, rules?: RowRules): number;
/** пиксели → минуты по шкале, со снапом в шаг перемещения */
declare function fromX(x: number, s: Scale, snap?: boolean): number;
/**
 * Куда встаёт КРАЙ полосы при ресайзе.
 *
 * Обычный снап в шаг шкалы тут врёт: на суточной сетке он кладёт край на
 * полночь, а бронь кончается в 12:00. Полоса после растягивания становится
 * длиннее на полдня, и щель на пересменку пропадает.
 *
 * Поэтому на сетке, где колонка — целый день, округляем НОМЕР ДНЯ относительно
 * своей отметки (заезд для левого края, выезд для правого) и возвращаем момент
 * уже с нужным временем. На часовых сетках отметок нет — там обычный снап.
 */
declare function snapEdge(x: number, s: Scale, edge: 'from' | 'to'): Moment;
/**
 * Минуты по шкале → момент `YYYY-MM-DDTHH:mm`.
 *
 * `asEnd` решает спор о границе. Конец рабочего дня и начало следующего — на
 * склеенной шкале ОДНА И ТА ЖЕ точка, и без подсказки она превращается в
 * «12:00 завтра». Для конца брони это неверно вдвойне: аренда кончилась в
 * 23:00, а не началась завтра.
 */
declare function toMoment(min: number, s: Scale, asEnd?: boolean): Moment;
/** сколько колонок в сетке целиком */
declare const totalCols: (s: Scale) => number;
/**
 * Верхний ряд шапки: колонки, слитые в группы. На суточной сетке группа —
 * месяц, на часовой — день. Ровно так это и делают в системах бронирования:
 * без верхнего ряда на часовой шкале невозможно понять, какой сейчас день,
 * а на месячной — какой месяц.
 */
declare function headGroups(s: Scale): Array<{
    label: string;
    span: number;
    at: Moment;
}>;
/** подписи колонок: момент начала каждой */
declare function columns(s: Scale): Array<Moment>;
/**
 * Пересекаются ли отрезки с учётом ЗАЗОРА.
 *
 * Зазор — не украшение: после бани полчаса уборки, и следующий сеанс в это
 * время поставить нельзя, хотя формально время свободно. У гостиницы ту же роль
 * играет разница между выездом и заездом, поэтому там зазор обычно нулевой.
 */
declare function conflicts(a: {
    from: Moment;
    to: Moment;
}, b: {
    from: Moment;
    to: Moment;
}, s: Scale, gapMin?: number): boolean;
/**
 * Границы суток, в которые попал момент (в минутах по шкале).
 *
 * Нужны там, где объект НОЧЬЮ ЗАКРЫТ: беседка сдаётся с 12:00 до 23:00, и
 * аренда «с двух дня до полудня следующего» не существует — между ними
 * одиннадцать часов, когда объекта нет. На круглосуточной шкале (окно = сутки)
 * это ограничение бессмысленно и не применяется.
 */
declare function dayBounds(min: number, s: Scale): {
    start: number;
    end: number;
};
/**
 * То же, но с оглядкой на СВОЁ окно строки: банкетный зал открывается в 14:00,
 * хотя сетка начинается в 10:00. Часы вне своего окна для строки — та же
 * «ночь», что и часы вне окна сетки: их не существует.
 */
declare function rowBounds(min: number, s: Scale, rules?: RowRules): {
    start: number;
    end: number;
};
/** закрывается ли объект на ночь: окно короче суток */
declare const closesAtNight: (s: Scale) => boolean;
/** живёт ли СТРОКА в пределах одного дня: окно сетки или своё окно короче суток */
declare const confined: (s: Scale, rules?: RowRules) => boolean;
/** длительность отрезка в минутах по шкале */
declare const lengthOf: (span: {
    from: Moment;
    to: Moment;
}, s: Scale) => number;
/**
 * Ограничить КРАЙ соседями: докуда его вообще пускают.
 *
 * Переносу при отказе ищут другое место — это правильно, там метили примерно.
 * Ресайзу так делать нельзя: тянут за край намеренно, и «перекинуть полосу за
 * соседа» вместо «упереть в него» — это уже не та бронь, которую растягивали.
 * Поэтому край просто останавливается там, где начинается чужое (плюс зазор).
 *
 * Возвращает минуты по шкале; `null` — упёрлись так, что не осталось и шага.
 */
declare function clampEdge(span: {
    id?: string;
    row?: string;
    from: Moment;
    to: Moment;
}, edge: 'from' | 'to', wantMin: number, others: Array<{
    id?: string;
    row?: string;
    from: Moment;
    to: Moment;
}>, s: Scale, gapMin?: number, rules?: RowRules): number | null;
/**
 * Сдвинуть отрезок в новое начало, сохранив длительность.
 *
 * Если объект закрывается на ночь, отрезок ЦЕЛИКОМ остаётся в своих сутках:
 * иначе беседку можно перетащить так, что аренда повиснет на закрытые часы.
 * Прижимаем к концу дня, а не отменяем перенос: тащили осмысленно, просто
 * промахнулись на пару часов.
 */
declare function moveTo(span: {
    from: Moment;
    to: Moment;
}, startMin: number, s: Scale, rules?: RowRules): {
    from: Moment;
    to: Moment;
};
/**
 * Раскладка по «этажам»: пересекающиеся отрезки не лежат друг на друге.
 *
 * Считается ПО ШКАЛЕ, а не сравнением строк. Сравнение строк работало, пока
 * выезд был раньше заезда (12:00 против 16:00): дата без времени лексикографически
 * меньше любого времени тех же суток, и «выехал — заехал» сходилось само собой.
 * Но у зала или коворкинга наоборот — заезд в 8:00, выезд в 20:00, — и тогда
 * такие брони считались непересекающимися, хотя накладываются на полдня.
 */
declare function stackFloors(spans: Array<{
    id: string;
    row: string;
    from: Moment;
    to: Moment;
}>, s: Scale, gapMin?: number | ((row: string) => number)): Map<string, number>;
/** сколько этажей понадобилось строке — по ним считается её высота */
declare function floorsPerRow(spans: Array<{
    id: string;
    row: string;
}>, floors: Map<string, number>): Map<string, number>;
/**
 * Готовые шкалы под три случая из шапки файла. Не «удобные пресеты», а
 * напоминание, что настроек ровно четыре и они не перепутываются.
 */
declare const SCALES: {
    /** сутки; заезд 16:00, выезд 12:00 */
    readonly hotel: (first: string, days: number, colW?: number) => Scale;
    /** почасовая, работа с 10:00 до полуночи */
    readonly sauna: (first: string, days: number, colW?: number) => Scale;
    /** час; дневная аренда с 12:00 до 23:00 */
    readonly gazebo: (first: string, days: number, colW?: number) => Scale;
    /**
     * Час; сетка на ВСЕ площадки базы разом — КРУГЛОСУТОЧНАЯ: баню арендуют и
     * ночью, поэтому резать ночь на уровне сетки нельзя. У кого ночи нет —
     * беседка до 23:00, банкетный зал с 14:00 — тот закрывает её СВОИМ окном
     * (`RowRules` на строке), и закрытые часы видны штриховкой. Минимум и зазор
     * тоже у строк: баня — от двух часов и полчаса уборки, пейнтбол — от часа
     * и час перезарядки.
     */
    readonly venues: (first: string, days: number, colW?: number) => Scale;
};

/** управление сеткой снаружи */
type TimelineApi = {
    /** подвести дату (или момент) к левому краю */
    scrollTo: (at: Moment) => void;
    /** подвести «сейчас» — то, что передано в `now`, иначе текущее время */
    scrollToNow: () => void;
    /** какой диапазон сейчас виден */
    visibleRange: () => {
        from: Moment;
        to: Moment;
    };
};
type TimelineRow = RowRules & {
    id: string;
    title: JSX.Element;
    /** группа, в которую строка попадёт: этаж, корпус, отдел */
    group?: string;
    /**
     * Чем торгует эта строка: сутками (номер) или часами (баня, пейнтбол).
     *
     * В одной шахматке они уживаются, и это нормально: у гостиницы с банями
     * ресурсы разные, а сетка одна. От типа зависит, что получится при выделении
     * пустого места — сутки с 16:00 до 12:00 или интервал в часах.
     */
    unit?: 'day' | 'hour';
};
type DumbTimelineProps<S extends Span = Span> = {
    rows: Array<TimelineRow>;
    spans: Array<S>;
    /**
     * Готовая шкала ЦЕЛИКОМ — например, из `SCALES`:
     * `scale={SCALES.hotel(start, 30)}`. Раньше пресет приходилось раскладывать
     * на восемь плоских пропсов, а `first`/`days`/`colW` передавать дважды —
     * рассинхрон был вопросом времени.
     *
     * Плоские пропсы (`from`, `days`, `stepMin`, …) работают ПОВЕРХ шкалы как
     * оверрайды: удобно взять пресет и подменить одно поле.
     */
    scale?: Partial<Scale>;
    /** первый день сетки и сколько дней показывать; побеждают `scale` */
    from?: string;
    days?: number;
    /**
     * Единица сетки. Сутки — частный случай, поэтому отдельного «режима» нет:
     *
     * - гостиница: `stepMin: 1440`, `checkIn: 960`, `checkOut: 720`;
     * - баня: `stepMin: 120`, окно `10:00…24:00`, зазор 30 мин;
     * - беседка: `stepMin: 60`, окно `12:00…23:00`.
     *
     * Готовые наборы лежат в `SCALES`.
     */
    stepMin?: number;
    /** рабочее окно дня в минутах от полуночи; вне окна сетки нет */
    dayStart?: number;
    dayEnd?: number;
    /**
     * Шаг перемещения, мин. Не задан — равен колонке. В бане сетка почасовая
     * (по ней читают время), а сеанс продаётся по два часа — вот это и есть
     * `snapMin: 120` при `stepMin: 60`.
     */
    snapMin?: number;
    /**
     * Зазор между соседями, мин. После бани полчаса на уборку: время формально
     * свободно, а ставить туда нельзя.
     */
    gapMin?: number;
    /** самая короткая бронь на всей сетке, мин; у строки бывает своя (`minMin` строки) */
    minMin?: number;
    /**
     * Перенесли или растянули. Применяет изменение ПОТРЕБИТЕЛЬ — у кита нет
     * своего состояния: не записал новые даты в `spans` (например, сервер
     * отказал) — полоса сама вернётся на место, потому что позиция всегда
     * считается из `spans`.
     *
     * `kind` говорит, что это было: перенос или растяжение за какой край —
     * «перенесено» и «продлено» для бизнеса разные события.
     */
    onChange?: (next: S, prev: S, kind: 'move' | 'resize-from' | 'resize-to') => void | boolean | Promise<void | boolean>;
    /**
     * Клик по полосе. Вторым аргументом — точка клика: по ней карточку ставят
     * РЯДОМ с бронью, а не по центру экрана. Модалка посреди шахматки закрывает
     * ровно ту бронь, о которой рассказывает.
     */
    onOpen?: (span: S, at: {
        x: number;
        y: number;
    }) => void;
    /** клик по пустой клетке — обычно «создать» */
    onEmptyClick?: (at: Moment, row: string) => void;
    /**
     * Протянули по пустому месту — обычно «создать на этот период».
     *
     * `needsTime` — предупреждение: строка почасовая, а сетка суточная, и точное
     * время из такого жеста не вытащить (в колонку шириной в сутки не прицелиться
     * в 14:00). Отдаём границы суток и честно говорим, что время надо уточнить
     * отдельно — спросив, а не угадав.
     */
    onRangeSelect?: (range: {
        row: string;
        from: Moment;
        to: Moment;
        needsTime: boolean;
    }) => void;
    /** ширина суток, px; по умолчанию 34 */
    colW?: number;
    /** высота строки, px; по умолчанию 34 */
    rowH?: number;
    /** ширина колонки с названиями, px; по умолчанию 200 */
    headW?: number;
    /**
     * Во что превращать дату БЕЗ времени: час заезда и час выезда, в минутах от
     * полуночи. В гостинице это 16:00 и 12:00 — тогда в день пересменки видно обе
     * брони и щель между ними, а не «занятый» день выезда.
     */
    checkIn?: number;
    checkOut?: number;
    /** подпись колонки; не задана — число месяца или время */
    dayLabel?: (at: Moment) => JSX.Element;
    /** подпись группы в верхнем ряду шапки; не задана — месяц или дата */
    groupLabel?: (at: Moment, span: number) => JSX.Element;
    /**
     * Строка сводки над сеткой: сколько свободно, выручка за день — что угодно.
     * В системах бронирования она первая, и смотрят на неё чаще, чем на сами
     * брони.
     */
    summary?: (at: Moment) => JSX.Element;
    /** заголовок сводки в левой колонке */
    summaryTitle?: JSX.Element;
    /** момент «сейчас» для вертикальной линии; не задан — линии нет */
    now?: Moment;
    /** пометить колонку: выходной, праздник, нерабочий час */
    dayClass?: (at: Moment) => string | undefined;
    /** содержимое полосы; не задано — просто подпись */
    children?: (span: S) => JSX.Element;
    /** не двигать и не растягивать — вся сетка целиком */
    readonly?: boolean;
    /**
     * Полоса, которую нельзя трогать, при том что остальные можно.
     *
     * Ради этого проп и появился: в шахматке рядом с бронями живут БЛОКИ —
     * ремонт, санитарный день, закрытие на профилактику. Они занимают место, но
     * не двигаются мышью и правятся в другом месте. Хранить их отдельным
     * массивом нельзя: они участвуют в проверке занятости наравне с бронями.
     */
    spanLocked?: (span: S) => boolean;
    /**
     * Класс на полосу. Через него красят СТАТУС (черновик, подтверждена, заезд
     * состоялся, отменена), помечают найденное поиском и подсвечивают групповую
     * бронь — одна компания на нескольких объектах.
     *
     * Именно класс, а не цвет пропом: статусов у каждого свои, и перечислять их
     * в ките значит навязывать чужую предметную область.
     */
    spanClass?: (span: S) => string | undefined;
    /** подсказка при наведении на полосу */
    spanTitle?: (span: S) => string | undefined;
    /**
     * Строка, в которой ничего нельзя создавать: объект закрыт, снят с продажи,
     * на консервации. Показывать её всё равно надо — иначе непонятно, куда делся
     * номер, — но выделение по ней не начинается.
     */
    rowDisabled?: (row: TimelineRow) => boolean;
    /** класс на строку целиком: приглушить закрытую, выделить свою */
    rowClass?: (row: TimelineRow) => string | undefined;
    /** правый клик по полосе: своё меню вместо браузерного */
    onSpanContextMenu?: (span: S, ev: MouseEvent) => void;
    /** правый клик по пустому месту */
    onEmptyContextMenu?: (at: Moment, row: string, ev: MouseEvent) => void;
    /**
     * Видимый диапазон изменился (прокрутили или сменили `from`/`days`).
     *
     * Нужен для догрузки: шахматка на год — это тысячи броней, и тянуть их все
     * ради экрана в тридцать дней незачем. Зовётся не чаще кадра.
     */
    onVisibleRange?: (range: {
        from: Moment;
        to: Moment;
    }) => void;
    /**
     * Управление снаружи: `api.scrollTo(day)` — подвести дату к левому краю,
     * `api.scrollToNow()` — к текущему моменту.
     *
     * Страница ресепшена висит открытой сутками, и «сегодня» на ней уезжает;
     * кнопка «Сегодня» — первое, чего от такой сетки ждут.
     */
    ref?: (api: TimelineApi) => void;
    /**
     * Показывать, ДОКУДА можно продлить: при наведении справа от полосы
     * проступает хвост до ближайшего соседа (с учётом зазора на уборку).
     *
     * По умолчанию ВЫКЛЮЧЕНО. На суточной сетке свободного места обычно недели, и
     * хвост на пол-экрана только мешает смотреть на брони. Включать имеет смысл
     * там, где место дорого и вопрос «дают ещё на час?» задают постоянно, — в
     * почасовых сетках.
     */
    showRoom?: boolean;
    class?: string;
    style?: JSX.CSSProperties;
};
declare function DumbTimeline<S extends Span>(props: DumbTimelineProps<S>): JSX.Element;

export { type Day, DumbTimeline, type DumbTimelineProps, type Moment, type RowRules, SCALES, type Scale, type Span, type TimelineRow, clampEdge, closesAtNight, columns, confined, conflicts, dayBounds, daysApart, floorsPerRow, fromX, headGroups, lengthOf, minLength, minutesOf, momentX, moveTo, rowBounds, shiftDay, snapEdge, snapOf, stackFloors, toMinutes, toMoment, toX, totalCols };
